{
  "name": "ops-data-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Data management web application with project-based Excel storage, versioning, data query (browse + AI Q&A) and LLM configuration.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c black,black \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web && npm run build -w server",
    "start": "NODE_ENV=production node server/dist/index.js",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
