做一个数据管理的web应用，先帮我规划一下技术栈和实现方式。

# 基本要求
-  所有界面均使用英文
- 技术栈应易于更新、迁移、运维
- 使用合适的数据库

  
# 模式选择
进入网站后可以选择数据管理 和 数据查询 两个模式。另外还有个配置页面。


# 数据管理
目的是管理项目、管理数据、管理数据版本等等。

- 可以新建若干个项目
- 可以设置项目名称和项目描述
- 选择项目的时候可以展示项目名称、描述和表头。
- 新建项目后可以上传一个excel（xls/xlsx文件），该文件的表头定义了这个项目的数据结构，并根据文件内容初始化数据。
- 可以选择已有的项目打开，显示数据。
	- 数据应该有基本的按列排序和过滤功能。
	- 不要分页
	- 太长的话（大于100行）则异步载入。
- 可以下载当前数据（xlsx版本）
- 可以增加数据的版本，比如1 -> 2，增加之后要求用户上传一个excel（xls/xlsx文件），需要解析这个文件的表头，是否和当前项目的表头一致。 如果不一致，则要求用户重新上传。
- 可以选择当前生效的版本。新增一个版本后会询问是否立即生效当前版本，以及提醒正在生效的版本。
* 可以删除某个版本
* 每个版本要记录创建时间
* 提供一个过滤条件，根据用户输入的键值对，对当前数据过滤。


# 数据查询
* 在项目列表中打开一个项目进行查询
* 在数据查询页面下可以切换两个模式，浏览模式和询问模式。
## 浏览模式
- 需要显示当前打开的项目
* 和数据管理一样有个数据查询页面显示数据。同样：
	- 数据应该有基本的按列排序和过滤功能。
	- 不要分页
	- 太长的话（大于100行）则异步载入。
- 可以下载当前数据（xlsx版本）
## 问答模式
* 需要显示当前打开的项目
* 界面是类似于AI大模型问答的对话框，下面输入和发送需求，上面出大模型返回的文字。
* 逻辑是这样的：
	1. 用户用文字输入和发送对这个项目里面的数据要求
	2. 调用配置页面配置的大模型转换为sql
	3. 执行sql，检索出结果。如果对于该项目的检索有结构化数据结果，则用表格显示。
	4. 转换过程、结果sql、执行过程、执行结果都需要告诉用户。
* 默认保存100条问答的历史记录和时间戳。

# 配置页面
- 进入配置页面默认需要输入密码： Abc123de
- 可以配置大模型的key和URL，以及选择模型，供问答模式使用